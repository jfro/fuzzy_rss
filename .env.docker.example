# Docker Compose Environment Variables
# Copy this file to .env to customize your Docker deployment
# 
# Usage:
#   cp .env.docker.example .env
#   # Edit .env with your values
#   docker-compose -f docker/postgres.yml up

# ============================================================================
# Server Configuration
# ============================================================================

PHX_HOST=localhost
PHX_URL_SCHEME=http               # Use "https" if behind a TLS proxy
PHX_URL_PORT=4000                 # Use "443" if behind a TLS proxy
PORT=4000

# ============================================================================
# Mail Configuration
# ============================================================================

# Default is "local" which shows a warning on login page
# Set to one of: smtp, mailgun, sendgrid, postmark, gmail
MAIL_ADAPTER=local

# From address for outgoing emails
MAIL_FROM_NAME=FuzzyRss
MAIL_FROM_EMAIL=noreply@example.com

# --------------------
# SMTP Configuration (when MAIL_ADAPTER=smtp)
# --------------------

# MAIL_SMTP_RELAY=smtp.gmail.com
# MAIL_SMTP_USERNAME=your-email@gmail.com
# MAIL_SMTP_PASSWORD=your-app-password
# MAIL_SMTP_PORT=587

# --------------------
# Mailgun Configuration (when MAIL_ADAPTER=mailgun)
# --------------------

# MAIL_MAILGUN_API_KEY=key-1234567890abcdef
# MAIL_MAILGUN_DOMAIN=mg.example.com

# --------------------
# SendGrid Configuration (when MAIL_ADAPTER=sendgrid)
# --------------------

# MAIL_SENDGRID_API_KEY=SG.xxxxxxxxxxxxxxxxxxxxx

# --------------------
# Postmark Configuration (when MAIL_ADAPTER=postmark)
# --------------------

# MAIL_POSTMARK_API_KEY=xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx
